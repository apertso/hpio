PORT=5000
JWT_SECRET=your_jwt_secret_key_here # Сгенерируйте длинную случайную строку
DB_HOST=localhost
DB_USER=SA
DB_PASSWORD=YourStrong!Password
DB_NAME=PaymentServiceDB
DB_PORT=1433
# Путь для хранения загруженных файлов и иконок. Относительный путь от корня backend папки.
UPLOAD_DIR=./uploads

# Storage configuration
STORAGE_TYPE=local  # or 'bunny' for Bunny.net CDN

# Bunny.net CDN settings (required only if STORAGE_TYPE=bunny)
BUNNY_STORAGE_API_KEY=your_api_key_here
BUNNY_STORAGE_ZONE=your_storage_zone_name
BUNNY_STORAGE_REGION=de  # Optional: de, ny, la, sg, se, uk
BUNNY_CDN_HOSTNAME=your-cdn-hostname.b-cdn.net  # Optional: for CDN URL generation